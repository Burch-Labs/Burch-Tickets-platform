workflows
ci.yml
name: Burch CI

on:
  push:
    branches:
      - main

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Repository Check
        run: echo "Burch Platform CI is running."
